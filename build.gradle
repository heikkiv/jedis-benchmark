apply plugin: 'java'
apply plugin:'application'

mainClassName = "jedisbenchmark.JedisBenchmark"

repositories {
    mavenCentral()
}

dependencies {
    compile 'redis.clients:jedis:2.5.2'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}
